export type AutoCompleteResult = {
    placeId: string
    streetNumber?: string
    streetName: string
    municipality: string
    postalCode: string
    freeformAddress: string
    country: string
    countryCode: string
}
